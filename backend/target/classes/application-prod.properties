spring.application.name=FitAI Workout

spring.datasource.url=jdbc:postgresql://postgres_db:5432/fitpersona
spring.datasource.username=fitatuser
spring.datasource.password=fitpassword123
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=FitAI2026SuperSecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=http://fitpersona.in,http://www.fitpersona.in,http://165.22.218.68

# OpenAI (optional)
app.openai.enabled=false
app.openai.api-key=${OPENAI_API_KEY:}

server.port=8080

logging.level.org.springframework.security=DEBUG

# ── AWS S3 ────────────────────────────────────────────────────────────────────
# Enabled in production; credentials are injected via docker-compose environment variables
aws.enabled=true
aws.accessKey=${AWS_ACCESS_KEY}
aws.secretKey=${AWS_SECRET_KEY}
aws.region=${AWS_REGION:ap-south-1}
aws.bucket=${AWS_BUCKET:fitpersona-storage}
